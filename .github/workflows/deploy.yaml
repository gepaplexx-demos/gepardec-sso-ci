name: Deploy via Helm

on:
  workflow_dispatch:


jobs:
  deployment:
    runs-on: 'ubuntu-latest'
    steps:
      - uses: actions/checkout@v1

      - name: 'Deploy'
        uses: 'vimeda/helm@v1.7.0'
        with:
          release: 'gepardec-sso-qa'
          namespace: 'gepardec-sso-qa'
          chart: 'apps/appuio/qa'
          token: '${{ github.token }}'
        env:
          KUBECONFIG_FILE: '${{ secrets.KUBE_CONFIG_QA }}'

